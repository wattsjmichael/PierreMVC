@using PierreMVC.Models;

<h1>Pierre's Order Guide</h1>
<ul>
@if (Model.Count == 0)
{
  <p>No orders yet! Time to do some hustling!</p>
}  
  @foreach (Order order in Model)
  {
    <li>@order.Description</li>
  }

</ul>
<a href="/orders/new">Add a new Order</a>