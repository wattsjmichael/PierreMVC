@using PierreMVC.Models;

<h1>Vendors</h1>
@if (Model.Count == 0)
{
  <h3>No Vendors have been added yet! Get to work!</h3>
}
@foreach (Vendor vendor in Model)
{
  <h3><a href='/vendors/@vendor.Id'>@vendor.Name</a></h3>
}
<p><a href='/'>Back to the homepage</a></p>